description = "Perform the whole process for commiting changes to the curret repo."

# The prompt uses !{...} to execute the command and inject its output.
prompt = """
1. Please generate a Conventional Commit message based on the following git diff:
```diff
!{git diff --staged}
```
2. Add changed files with git add command
3. Commit changes using the diff output from step 1
"""